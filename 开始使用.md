# 开始使用（非技术人员版）

如果你的个人任务是 Choice OpenBB Provider 插件，请先阅读
`extensions/openbb_choice/README.md`。根目录原有 Notebook 仍用于落库和客户端闭环验证。

1. 解压整个项目文件夹，不要只单独拿出 Notebook。
2. 在 VS Code 选择“文件 → 打开文件夹”，打开 `qianji_openbb_mini`。
3. 覆盖补丁后先运行`notebooks/00_openBB环境构建.ipynb`并重启内核；首次模拟演示再打开`01_本地小型数据基座演示.ipynb`。
4. 右上角选择 Python 内核，然后从第一格开始逐格运行。
5. 模拟数据成功后，打开项目根目录的 `.env`：
   - 使用 Tushare 就填写 `TUSHARE_TOKEN`；
   - 使用 iFinD 就填写 `IFIND_REFRESH_TOKEN`；
   - Wind 和 Choice 需要技术人员先让官方 SDK 在同一个 Python 环境中可导入。
6. 回到 Notebook，把 `SOURCE_TO_RUN = "mock"` 改成相应数据源名称，只测试一个代码。
7. 所有行情会写入 `data/qianji_market.db`，Excel 会生成在项目根目录。

如果出现错误，不要重复点很多次。把 Notebook 的完整红色报错、使用的数据源、代码和日期范围发给技术人员即可。不要截图或发送 Token、账号密码。

## 当前已验证

- 模拟源下载、标准化、SQLite 幂等写入：通过；
- Python 查询：通过；
- REST 模拟客户端：通过；
- Excel 导出：通过；
- OpenBB `provider="qianji"` 读取 SQLite：通过；
- Choice证券主数据、沪深交易日历幂等落库：通过模拟SDK自动测试；
- 自动化测试：24 项通过。

## 尚需在你的电脑验证

- WindPy 是否能被当前 VS Code Python 导入；
- EmQuantAPI 是否完成激活并能返回 Choice 数据；
- iFinD refresh_token、接口权限和 IP 白名单；
- Tushare Token 和所需接口积分；
- 四家合同是否允许所需范围的批量下载和本地存储。
