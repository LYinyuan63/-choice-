# Choice长期交易日历与主数据增量刷新（qianji-data-mini 0.6.0）

本补丁为Choice参考数据增加“长期覆盖 + 可追溯增量”的刷新能力。

## 新增能力

- 交易日历起止日期可配置，默认从2010-01-01刷新至快照日；
- 每次保存Choice全A板块成员快照；
- 与上一期快照比较后记录`added`、`removed`和`modified`；
- 快照同时保存主数据摘要，保证同一日期重复运行仍得到相同变更结论；
- 主数据、交易日历、成员快照和变更记录均采用幂等写入；
- 单独保留刷新运行日志，记录数量、时间和错误信息。

`removed`只表示证券退出当前Choice板块快照，不等同于退市。程序不会据此直接把证券状态改成`delisted`；退市状态仍由Choice明确返回的退市日期判断。

## 新增SQLite表

| 表 | 作用 | 幂等主键 |
|---|---|---|
| `security_universe_snapshot` | 保存每期全A成员和主数据摘要 | 来源、板块、快照日、证券代码 |
| `security_master_change` | 保存新增、退出板块和字段变更 | 来源、板块、快照日、证券代码、变更类型 |
| `reference_refresh_run` | 保存每次刷新运行证据 | 自增运行ID |

已有`security_master`和`trading_calendar`表继续复用，不需要更换数据库。

## 安装和运行

1. 关闭正在运行的Notebook内核；
2. 把0.6.0补丁内容覆盖到原`qianji_openbb_mini`项目根目录；
3. 运行`notebooks/00_openBB环境构建.ipynb`；
4. 确认`qianji-data-mini`显示为`0.6.0`；
5. 彻底重启Notebook内核；
6. 运行`notebooks/08_Choice长期交易日历与主数据增量刷新验收.ipynb`。

不能只复制08号Notebook；旧版本没有三张新增表和快照刷新函数。

## 默认参数

- 全A板块：`001004`
- 快照名称：`all_a_001004`
- 市场：`CNSESH`、`CNSESZ`
- 长期日历开始日：`2010-01-01`
- 快照日及日历结束日：默认使用本机昨天

首次运行会把现有`security_master`中更早一期的在市股票作为基线快照。若数据库没有历史主数据，则本次结果作为首期基线，不会把全市场误报成五千多条“新增”。

## 验收解释

- `added`：本期在板块中、上一期不在；
- `removed`：上一期在板块中、本期不在，仅记录成员变化；
- `modified`：两期都在，但名称、交易所、资产类型、币种、上市/退市日或状态有变化；
- `unchanged`：两期都在且上述字段不变；
- 同一天运行两次：快照、变更、主数据和日历行数不应继续增长；运行日志增加一条属于正常审计记录。

本补丁自动化测试为30项全部通过。
