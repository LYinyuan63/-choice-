# Tushare 与 Choice 真实数据自动验证

这个脚本只下载一个很小的验证集：默认两只股票、最近约35天。它会自动完成真实接口调用、SQLite落库、质量检查、双源对比和Excel证据导出，不会把凭据写入报告。

## 1. 安装项目

在 VS Code 打开本文件夹，选择你平时使用的虚拟环境，然后在终端运行：

```powershell
python -m pip install -e .
```

Choice 还需要先把官方 EmQuantAPI 安装到同一个虚拟环境。Tushare 会随本项目安装。

## 2. 配置凭据

复制 `.env.example`，命名为 `.env`。两种 Choice 登录方式任选一种。

### 推荐：Choice userInfo 登录

先运行官方 `LoginActivator.exe` 完成手机号验证，再填写：

```text
TUSHARE_TOKEN=你的TushareToken
CHOICE_LOGIN_MODE=userinfo
```

### 可选：Choice 账号密码登录

新版 EmQuantAPI 支持账密登录：

```text
TUSHARE_TOKEN=你的TushareToken
CHOICE_LOGIN_MODE=password
CHOICE_USERNAME=你的Choice账号
CHOICE_PASSWORD=你的Choice密码
```

`.env` 已被 `.gitignore` 排除。不要截图、转发或上传这个文件。如果密码包含逗号或换行，请改用 userInfo 登录。

## 3. 运行

最简单的方法是双击：

```text
运行真实数据验证.bat
```

也可以在 VS Code 终端运行：

```powershell
python "clients/自动获取验证数据.py"
```

## 4. 输出

运行后查看 `validation_output`：

- `真实数据验证证据.xlsx`：验证汇总、数据地图、质量检查、落库统计、跨源对比和源数据；
- `真实数据验证结果.json`：机器可读的执行结果；
- `data/qianji_market.db`：真实落库数据。

Excel 中不会保存 Tushare Token、Choice账号或Choice密码。

## 5. 可调整范围

在 `.env` 中可以修改：

```text
VALIDATION_SOURCES=tushare,choice
VALIDATION_SYMBOLS=000001.SZ,600000.SH
VALIDATION_START_DATE=2026-08-01
VALIDATION_END_DATE=2026-08-31
```

第一次不要增加大量股票。先以两只股票验证接口权限、字段口径和落库结果。

## 6. 常见结果

- Tushare PASS、Choice FAIL且提示无法导入：EmQuantAPI没有安装到当前虚拟环境。
- Choice登录错误：确认API权限、账号密码或本机userInfo激活状态。
- Choice数据请求错误：账号可能已能登录，但没有相应行情字段权限。
- 两源成交量比例接近100：Choice的成交量单位可能是“手”，需要核实后设置 `CHOICE_VOLUME_MULTIPLIER=100` 再运行。
