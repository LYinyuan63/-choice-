# 应用Choice额度保护补丁 v0.8.1

本补丁针对真实运行中出现的`10001029: data limit exceeded`增加保护。

## 修改内容

- 首个财务请求触发10001029后，停止本轮剩余财务请求；
- 分红链路保持独立，财务额度受限时仍可尝试完成；
- 终端明确区分“本次接收/写入”与“数据库累计”；
- Excel新增`current_run_summary`和`current_run_errors`工作表；
- 有错误时以简洁提示和退出码2结束，不再输出长Python堆栈。

## 使用方法

1. 停止继续运行旧版客户端；
2. 把补丁解压到`qianji_openbb_mini`根目录并覆盖同名文件；
3. 运行`notebooks/00_openBB环境构建.ipynb`；
4. 确认版本为`0.8.1`后彻底重启内核；
5. 先登录Choice量化API官网的“流量查询”确认额度，再决定是否运行客户端。

`10001029`由Choice服务端返回，补丁不能增加或恢复账号额度。额度总量、统计周期、重置时间及财务CTR是否单独计费，应以账号页面或Choice客户经理答复为准。
